lambda함수 (익명함수)

lambda 매개변수(아무거나) : 표현식

10 + 20의 계산

>>>(lambda x, y: x+y)(10,20)
30

def 함수이름(매개변수):
	return 결과
를 람다 표현식으로 쓰면
lambda 매개변수 : 결과
익명함수이기 때문에 이름을 지정하지 않는다!
하지만 이름이 없고 저장된 변수가 없어서 다시 사용할 수는 없다..(해주려면 정의해줘야함)


def add(x):
	return x+10
add(10)
20
를 람다 표현식으로 쓰면

add = lambda x: x+10
add(10)
20